# Alliance Street Accountancy - Website

## Project Overview
A professional marketing website for Alliance Street Accountancy Ltd, a UK-based accounting firm. It is a modern, responsive single-page application (SPA) designed to showcase accounting services and generate leads.

## Tech Stack
- **Framework**: React 19 with TypeScript
- **Build Tool**: Vite 8
- **Styling**: Tailwind CSS 4 (via Vite plugin)
- **Animations**: Framer Motion + GSAP (with ScrollTrigger)
- **Video**: HLS.js for streaming video
- **Package Manager**: npm

## Project Structure
- `src/` — React source code (App.tsx, main.tsx, index.css, assets/)
- `public/` — Static assets (logo, favicon, PDF checklist)
- `scripts/` — Python script for PDF generation (`build-pdf.py`)
- `dist/` — Built output (generated by `npm run build`)

## Design Theme
- Inspired by alliancestreet.ae color template
- **Hero**: Dark video background + red radial gradient overlay, dark nav with pill-shaped bordered links, white hero card with rounded top corners and dark/red bar at bottom, dark text and black CTA buttons
- **Sections (Trust → Contact)**: White/light background, dark text, light gray borders/cards, red accent (#E40014)
- **Footer**: Kept dark (dark video overlay) using `.dark-section` CSS class
- **Loading screen**: Kept dark with explicit inline styles

## Development
- **Dev server**: `npm run dev` (port 5000, host 0.0.0.0)
- **Build**: `npm run build`
- **Lint**: `npm run lint`

## Deployment
Configured as a **static** deployment:
- Build command: `npm run build`
- Public directory: `dist`
